# Källmaterial

Här sparas originalmaterial som används för att skapa webboptimerade assets.

- `cases/` innehåller råa PNG-captures från de interaktiva projekten.
- Publicerade WebP-varianter skapas med `npm run assets:optimize`.
- Filer i den här mappen importeras inte direkt av webbplatsen.
